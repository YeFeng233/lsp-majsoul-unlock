.gradle/
build/
local.properties
.idea/
*.iml
.cxx/
app/src/main/jniLibs/
# Cargo.lock is deliberately NOT ignored: majsoul-jni is a binary crate, so the
# lockfile is what makes a release build reproducible.
rust/majsoul-jni/target/
hook-probe/rust-modder/target/
external/hev-socks5-tunnel/libs/
external/hev-socks5-tunnel/obj/
.DS_Store
release-assets/

# Local build caches, logs, and signing credentials
.gradle-user-home/
.gradle-user/
.android/
.android-user/
*.log
ci-keystore.jks
*.keystore
.signing/

# The upstream proxy CA private key is not required by the Android hook.
external/MajsoulMax-rs/src/ca/hudsucker.key
